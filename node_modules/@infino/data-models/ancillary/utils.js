const { snakeCase } = require( 'change-case' );

exports.transformCase = schema => {

    return Object.keys( schema ).reduce( ( acc, next ) => {

        acc[ snakeCase( next ) ] = schema[ next ];
        return acc;

    }, {} );

};
