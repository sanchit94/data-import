const { DataTypes } = require( 'sequelize' );

module.exports = {

    id: {
        allowNull: false,
        primaryKey: true,
        type: DataTypes.UUID,
    },

    giftcard_id: {
        allowNull: false,
        type: DataTypes.UUID,
        onDelete: 'CASCADE',
        references: {
            model: 'giftcards',
            key: 'id',
        }
    },

    min: {
        allowNull: false,
        type: DataTypes.INTEGER
    },

    max: {
        allowNull: false,
        type: DataTypes.INTEGER
    },

    created_at: {
        allowNull: false,
        type: DataTypes.DATE,
        defaultValue: DataTypes.NOW,
    },

    updated_at: {
        allowNull: false,
        type: DataTypes.DATE,
    }

};
