const { Giftcard } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'giftcards', Utils.transformCase( Giftcard ) ),
    down: queryInterface => queryInterface.dropTable( 'giftcards' )

};
