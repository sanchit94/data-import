const { Tag } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'tags', Utils.transformCase( Tag ) ),
    down: queryInterface => queryInterface.dropTable( 'tags' )

};
