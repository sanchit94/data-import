const { Session } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'sessions', Utils.transformCase( Session ) ),
    down: queryInterface => queryInterface.dropTable( 'sessions' )

};
