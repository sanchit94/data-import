const { Bank } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'banks', Utils.transformCase( Bank ) ),
    down: queryInterface => queryInterface.dropTable( 'banks' )

};
