const { Integration } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'integrations', Utils.transformCase( Integration ) ),
    down: queryInterface => queryInterface.dropTable( 'integrations' )

};
