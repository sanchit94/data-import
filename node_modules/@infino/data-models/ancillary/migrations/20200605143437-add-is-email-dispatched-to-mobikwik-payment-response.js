const { DataTypes } = require( 'sequelize' );

module.exports = {

    up: queryInterface => queryInterface.addColumn( 'mobikwik_payment_responses', 'is_email_dispatched', {
        type: DataTypes.BOOLEAN,
        defaultValue: true,
    } ),

    down: queryInterface => queryInterface.removeColumn( 'mobikwik_payment_responses', 'is_email_dispatched' )

};
