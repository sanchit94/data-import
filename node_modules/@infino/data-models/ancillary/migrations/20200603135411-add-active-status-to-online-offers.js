const { DataTypes } = require( 'sequelize' );

module.exports = {

    up: queryInterface => queryInterface.addColumn( 'online_offers', 'active_status', {
        type: DataTypes.BOOLEAN,
        defaultValue: true,
    } ),

    down: queryInterface => queryInterface.removeColumn( 'online_offers', 'active_status' )

};
