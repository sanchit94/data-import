const { CuratedGiftcard } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'curated_giftcards', Utils.transformCase( CuratedGiftcard ) ),
    down: queryInterface => queryInterface.dropTable( 'curated_giftcards' )

};
