const { DataTypes } = require( 'sequelize' );

module.exports = {

    up: queryInterface => queryInterface.addColumn( 'orders', 'temporary_giftcard_details', {
        type: DataTypes.JSONB,
        allowNull: true
    } ),

    down: queryInterface => queryInterface.removeColumn( 'orders', 'temporary_giftcard_details' )

};
