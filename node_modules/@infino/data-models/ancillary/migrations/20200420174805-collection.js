const { Collection } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'collections', Utils.transformCase( Collection ) ),
    down: queryInterface => queryInterface.dropTable( 'collections' )

};
