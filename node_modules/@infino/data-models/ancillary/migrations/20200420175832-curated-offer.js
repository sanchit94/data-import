const { CuratedOffer } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'curated_offers', Utils.transformCase( CuratedOffer ) ),
    down: queryInterface => queryInterface.dropTable( 'curated_offers' )

};
