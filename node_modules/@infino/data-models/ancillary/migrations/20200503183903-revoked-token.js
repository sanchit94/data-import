const { RevokedToken } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'revoked_tokens', Utils.transformCase( RevokedToken ) ),
    down: queryInterface => queryInterface.dropTable( 'revoked_tokens' )

};
