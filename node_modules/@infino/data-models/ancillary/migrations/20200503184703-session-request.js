const { SessionRequest } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'session_requests', Utils.transformCase( SessionRequest ) ),
    down: queryInterface => queryInterface.dropTable( 'session_requests' )

};
