const { User } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'users', Utils.transformCase( User ) ),
    down: queryInterface => queryInterface.dropTable( 'users' )

};
