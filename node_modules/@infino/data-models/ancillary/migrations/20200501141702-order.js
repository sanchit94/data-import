const { Order } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'orders', Utils.transformCase( Order ) ),
    down: queryInterface => queryInterface.dropTable( 'orders' )

};
