const { Provider } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'providers', Utils.transformCase( Provider ) ),
    down: queryInterface => queryInterface.dropTable( 'providers' )

};
