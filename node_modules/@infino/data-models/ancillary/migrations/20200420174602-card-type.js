const { CardType } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'card_types', Utils.transformCase( CardType ) ),
    down: queryInterface => queryInterface.dropTable( 'card_types' )

};
