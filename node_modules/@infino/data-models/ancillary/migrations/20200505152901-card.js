const { Card } = require( '../schema' );
const Utils = require( '../utils' );

module.exports = {

    up: queryInterface => queryInterface.createTable( 'cards', Utils.transformCase( Card ) ),
    down: queryInterface => queryInterface.dropTable( 'cards' )

};
