const { Sequelize: { Model } } = require( 'sequelize' );
const { Provider: ProviderSchema } = require( '../schema' );

class Provider extends Model {}

module.exports = sequelize => {

    Provider.init( ProviderSchema, { sequelize, tableName: 'providers', underscored: true, timestamps: true } );
    return Provider;

};
