const { Sequelize: { Model } } = require( 'sequelize' );
const { Bank: BankSchema } = require( '../schema' );

class Bank extends Model {}

module.exports = sequelize => {

    Bank.init( BankSchema, { sequelize, tableName: 'banks', underscored: true, timestamps: true } );
    return Bank;

};
