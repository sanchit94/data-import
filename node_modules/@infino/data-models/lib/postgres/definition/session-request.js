const { Sequelize: { Model } } = require( 'sequelize' );
const { SessionRequest: SessionRequestSchema } = require( '../schema' );

class SessionRequest extends Model {}

module.exports = sequelize => {

    SessionRequest.init( SessionRequestSchema, { sequelize, tableName: 'session_requests', underscored: true, timestamps: true } );
    return SessionRequest;

};
