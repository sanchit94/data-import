const { Sequelize: { Model } } = require( 'sequelize' );
const { Integration: IntegrationSchema } = require( '../schema' );

class Integration extends Model {}

module.exports = sequelize => {

    Integration.init( IntegrationSchema, { sequelize, tableName: 'integrations', underscored: true, timestamps: true } );
    return Integration;

};
